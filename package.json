{
  "name": "learning-web-codemods",
  "version": "0.1.0",
  "scripts": {
    "start": "npm run build && ./build/bin/cli.js",
    "test": "codemod-cli test",
    "update-docs": "codemod-cli update-docs",
    "build": "tsc --pretty",
    "watch": "tsc -w --pretty"
  },
  "bin": "./build/bin/cli.js",
  "keywords": [
    "codemod-cli"
  ],
  "dependencies": {
    "@types/commander": "^2.12.2",
    "codemod-cli": "^0.2.10",
    "commander": "^2.19.0"
  },
  "devDependencies": {
    "@types/jscodeshift": "^0.6.0",
    "@types/node": "^11.11.3",
    "jest": "^24.1.0",
    "ts-node": "^8.0.3",
    "typescript": "^3.3.3333"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
